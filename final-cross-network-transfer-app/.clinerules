# 跨网传递项目 - Cline 长期工程规则

## 1. 工作方式规则
- 新任务开始时，先理解项目状态，再执行具体操作
- 在大任务开始前，先检查当前目录、环境、关键路径和已有状态
- 先读记忆文件，再和真实代码交叉验证
- 每完成一个阶段性里程碑，提醒更新 `_project_memory` 中的状态文件
- 遇到报错时，先分类判断是：
  - 命令写法错误（PowerShell vs CMD）
  - 路径错误
  - 环境错误
  - 配置错误
  - 数据问题
  - 前端交互问题
  - 后端逻辑问题
  - 部署平台问题（Render/GitHub）
- 不要把表层命令报错误判成深层工程逻辑错误
- 优先做最小修复，不要无端扩大改动范围
- 不要输出超长无意义日志，优先总结关键信息

## 2. 新会话上下文恢复规则
- 每次新会话开始时，优先读取：
  - `_project_memory/project_status.md`
  - `_project_memory/project_status.txt`
  - `_project_memory/error_memory.md`
- 读取后先简要汇报当前项目状态，再执行新任务
- 不要忽略记忆文件中的：
  - 已完成事项
  - 已解决问题
  - 已知风险
  - 下一步建议
  - 历史错误处理方式
- 对"计划中的功能"必须再次与代码交叉核对，不要默认已完成

## 3. 文件操作规则
- 修改文件前，先确认文件的用途和影响范围
- 如果是关键配置文件，修改前先记录"修改前状态"和"修改原因"
- 如果是核心业务逻辑文件，除非用户明确授权，否则不要擅自大改
- 新创建的辅助文件、记忆文件放在 `_project_memory` 目录
- 更新记忆文件时，必须标注哪些内容是"已验证"，哪些是"待确认/待实现"

## 4. 终端与命令规则
- 当前系统：Windows 11，使用 PowerShell
- 切目录使用 `cd ...` 或 `Set-Location ...`
- 多条命令使用 `;` 分隔
- 设置环境变量使用 `$env:VAR=value`
- 不要使用 CMD 风格的命令

## 5. Render / GitHub / 部署规则
- 本地能跑 ≠ Render 一定能跑
- Render 问题先区分：Build 失败 / Start 失败 / Health Check 失败 / 配置错误
- Render Web Service 使用 `render.yaml` 配置
- 遇到 "The application exited early"，优先检查：
  - PORT 环境变量是否设置
  - NODE_ENV 是否正确
  - 全局异常处理是否完善
  - Node 版本兼容性
- Node 版本要求：>=18 <=22，推荐使用 Render 默认的 Node 20 LTS
- 遇到 `Could not read package.json`，优先检查 Root Directory 配置
- 默认不要让工具自动 push 到 `main`，除非用户明确授权

## 6. 当前项目特定规则
- 当前项目是跨网络网页传输工具
- 核心链路：房间 -> 消息 -> 文件/图片上传 -> 邀请链接 -> 上线部署
- 技术栈：Node.js + Express + Socket.IO + Multer
- 存储：JSON 文件 + 磁盘持久化
- 端口：本地 3000，Render 10000

## 7. Git 工作流规则
- 功能开发使用 feature 分支
- 分支命名：`feature/功能名` 或 `fix/问题名`
- 合并前本地测试通过
- PR 合并后 Render 自动部署

## 8. 记忆维护规则
- 每次完成阶段性任务后，考虑更新：
  - `project_status.md`
  - `project_status.txt`
  - `error_memory.md`
- 如果某个错误已确认修复，应写入 `error_memory.md`
- 如果项目进入新阶段，应更新状态文件
- 不要让记忆文件长期停留在过期状态
